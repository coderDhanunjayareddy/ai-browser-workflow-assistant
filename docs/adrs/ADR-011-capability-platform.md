# ADR-011: Capability Platform

## Status

Accepted.

## Decision

The Capability Registry expands into a Capability Platform covering registry, discovery, versioning, health, permissions, constraints, and metrics.

## Consequences

Only existing browser and user capabilities are registered in V3.0. Future capability types can be added without changing planner logic.
